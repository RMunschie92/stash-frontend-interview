/**
 * @fileoverview Namespace for defining types and interfaces used in the application.
 */
export type Hotel = {
  name: string;
  city: string;
  daily_rate: number;
  id: number;
  has_member_rate: boolean;
  image: string;
};
